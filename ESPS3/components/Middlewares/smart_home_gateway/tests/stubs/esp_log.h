#ifndef ESP_LOG_H
#define ESP_LOG_H

#define ESP_LOGI(tag, format, ...) do { (void)(tag); (void)(format); } while (0)
#define ESP_LOGD(tag, format, ...) do { (void)(tag); (void)(format); } while (0)
#define ESP_LOGW(tag, format, ...) do { (void)(tag); (void)(format); } while (0)

#endif
